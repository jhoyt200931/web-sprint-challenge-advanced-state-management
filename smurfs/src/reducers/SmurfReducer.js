

const initialState = {
    name: '',
    age: 1,
    height: '',
    id: 1
}


export const SmurfReducer = (state = initialState, action) => {
    switch(action.type) {

        default: 
        return state;
    }
}